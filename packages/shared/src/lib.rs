//! Wire-contract types, Rust crate root.
//!
//! Append point: later steps add module files here and re-export them —
//! never edit another step's module (wire.md conventions). The types
//! themselves live at the package root (`types.rs`) beside their TS mirror.

#[path = "../types.rs"]
pub mod types;
