# Rust
target/

# Node
node_modules/
dist/

# Env / secrets — never commit. An .env.example may be tracked.
.env*
.dev.vars*
!.env.example
!.dev.vars.example

# Wrangler local state / caches
.wrangler/
cache/
out/

# Editors / OS
.DS_Store
*.log
# Spike gate evidence is load-bearing record (review round 1, blocking fix) —
# keep its logs tracked even though *.log is ignored elsewhere.
!spike/evidence/**
