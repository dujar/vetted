[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.28"
evm_version = "cancun"
remappings = ["forge-std/=lib/forge-std/src/"]
# the pattern tripwire reads the genuine forwarder bytes out of the spike
# evidence directory (committed by step 2) — read-only, nothing else
fs_permissions = [{ access = "read", path = "../../../spike/evidence" }]
