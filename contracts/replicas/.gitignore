# Foundry deps are restored with `forge install` inside this directory
# (reads .gitmodules + foundry.lock). Never commit lib/ — same discipline
# as the spike (step-2 findings).
lib/
# per-run pointer rewritten by every local broadcast — run-<ts>.json is the
# stable receipt we track
broadcast/**/run-latest.json
