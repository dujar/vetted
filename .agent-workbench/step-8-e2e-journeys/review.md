# Step 8 review — round 1 (2026-09-21, branch step-8-e2e-journeys @ 325029b vs main b889011)

Verified by running: frontend vitest 84/84, shared 21/21, `npm run build` exit 0, `npx playwright test` in e2e/ = 28 passed + 3 skipped (all three activation-gated, each skip carrying its reason + runbook pointer), `tsc --noEmit` in e2e/ clean.

## Coverage verified

- **J1** (journeys.md:6–22): typed + deep link (both entry formats), compare VERIFIED-vs-IMPOSTOR, wallet-free pinned via `window.ethereum === undefined` (scan.spec.ts:77–81), all four verdicts, not-a-contract, off-pattern + depth-boundary copy, degraded banner, RPC retry, wrong-network read-only notice + switch-back. Progress-lines-in-order lives in the live spec (scan.live.spec.ts:41–79), where the transport can be held open — the only place that panel is observable.
- **J2** (journeys.md:24–34): stub EIP-1193 wallet (EIP-6963 + window.ethereum, 4001 reject / -32603 gas) through wagmi `injected()`; wrong-network prompt asserted BEFORE quoting (no guard-checks text, swap.spec.ts:32–41); settle with receipt; all five `GUARD_*` asserted byte-exact as a data-driven loop over `GUARD_REVERT_REASONS` imported from vetted-shared (swap.spec.ts:63–88) — no retranscription anywhere; funds-never-move wording asserted per reason. On-chain byte-exact reverts + settle are the activation-gated live spec, pinned to the shared literals (`expect(GUARD_REVERT_REASONS).toContain(entry.reason)`, swap.live.spec.ts:138) — correctly gated, not a blocker.
- **J3** (journeys.md:36–45): table + REVOKED red + drill-in with revocation tx, criteria panel with `SELECTORS.getRecord` from vetted-shared, row deep-link; degraded forced by intercepting every registry eth_call (registry.live.spec.ts:17–27).
- **Honest degrade** (Revised 2026-09-20 note 2): mock RPC_RETRYABLE sentinel; live 503 → backoff → retry → VERIFIED with the request count asserted (scan.live.spec.ts:81–105); `/scan never fabricates` (VERIFIED ⇒ evidence rows) and `/watchdog` degrade-sentinel payload tests hit the real worker, skipping honestly when unreachable; scratch specs assert no IMPOSTOR and no ABSENT rows off-4663 (LE 7).
- **N7** minimal and correct: api.ts re-exports `WatchdogStats` from vetted-shared (whose `provenanceUrl: string | null` I confirmed is the canonical shape the worker serves), mock source adds the published-baseline URL, api.test.ts expectations updated (LE 5).
- **Sanctioned wiring** all behavior-preserving at the product default: `VETTED_CHAIN` falls back to robinhoodChain with `VITE_CHAIN_ID` unset; swap gate/copy templates reproduce "Robinhood Chain · 4663" byte-identically; `injected()` only under `VITE_E2E_STUB_WALLET=1`; `VITE_REGISTRY_TOKENS` unset → KNOWN_TOKENS (`name || symbolLive` is a no-op for the product table). The live scratch pass is double-gated (tolerant deployments loader + worker `record != null` precondition) with the runbook in e2e/README.md — correctly not run, not hand-forged.
- **All 10 verify loose ends** applied as disclosed (LE 1–4 sanctioned edits, LE 5 test update, LE 6 degraded route-forced, LE 7 IMPOSTOR mock-only, LE 8 seed wires buyer/MM balances+allowances, LE 9 local/on-demand with no workflow file, LE 10 tolerant loader + skip-with-runbook). `reconciled:` correctly left empty. findings.md's touched-file list matches the actual 33-file diff exactly; no contracts/scan-backend/shared/demo/.github changes.

## Findings (all non-blocking)

1. [non-blocking] frontend/src/pages/ScanPage.tsx:35–42, SwapPage.tsx:33–36, RegistryPage.tsx:25–27 — a FIFTH frontend change beyond the four sanctioned extensions: default-param sources replaced with lazy module singletons ("real fix the journeys exposed", disclosed in findings.md). What breaks if reverted: a fresh source instance per render re-fires the effects in an infinite loop, so no journey spec can run against the real bundle — same journey-blocking class the four sanctioned edits were granted for. Minimal, unit tests inject stable props so 84 stay green, disclosed prominently. Needs the coordinator to ratify it as an effective fifth sanctioned extension; reverting is not a viable fix.
2. [non-blocking] e2e/fixtures/constants.ts:82 — `RETRY_MAX_ATTEMPTS = 3` is exported and the README claims "retry with backoff … up to RETRY_MAX_ATTEMPTS", but no spec references it; both retry tests do exactly one backoff retry. Constant + README line overstate the implemented policy — wire a loop in the live retry spec or drop the constant and amend the sentence.
3. [non-blocking] J3's "empty registry state" (journeys.md:45) is not exercised e2e — it is pinned by frontend/tests/registry.test.tsx:76 ("empty: no records yet + scan pointer"), as the spec header discloses. Defensible split (the mock source has no empty sentinel; forcing one would need an out-of-scope frontend hook), but note it in the reconcile sweep since the journeys contract asks for e2e on every listed unhappy path.
4. [non-blocking] e2e/README.md and e2e/playwright.config.ts:4–5 say the servers run on :4173/:4174; actual defaults are 4883/4884 (MOCK_PORT/LIVE_PORT, constants.ts:74–75). Stale doc numbers.
5. [non-blocking] e2e/scripts/seed-scratch.sh:20–21 — the header comment lists the red-flag order "PAUSED (tokenA) → IMPL_MISMATCH", contradicting the entries and the (correct) body comment at :102–105, which replay IMPL_MISMATCH before PAUSED per guard order. Fix the header line.
6. [non-blocking] e2e/specs/scan.live.spec.ts:78 — the UI-side degrade-sentinel assertion `getByText("0", { exact: true })` is unscoped, so it could match any lone "0" on the page; scope it to the watchdog widget the way scan.spec.ts:52 does.

## Verdict

APPROVED — the journeys do what the plan said, the tests assert the real thing (byte-exact via shared constants, not smoke), the skips are honest and recorded, and the diff is file-disciplined except the disclosed singleton fix (finding 1, ratification not reversion).
