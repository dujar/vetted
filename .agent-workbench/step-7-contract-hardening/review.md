# Step 7 review — 2026-09-21 (round 1)

Branch `step-7-contract-hardening` (fee70a5, 4 commits) vs base `main` (b889011). Diff: 11 files — guard/lib.rs +418, registry/lib.rs +298, guard+registry+hello Cargo.toml, contracts/Cargo.lock, docs/criteria.md + threats.md + gas-report.md (new), step-7 findings.md + verify.md.

## Verified

- **Tests are real and green (run, not trusted).** `cargo test --workspace` exit 0: registry 14 (2 new fuzz), guard 30 (9 new fuzz), hello 2. wasm32 `cargo check` clean on all three crates, zero warnings in lib builds.
- **Fuzz suites assert properties, not vacuity.** Registry: one model-based sequence property (10k, fixed ChaCha) predicts success/error variant + exact seven-field state after every op from an independent mirror model — subsumes revert-leaves-zero-state, covers NotRegistrar on all three writes, NoRecord, ZeroRegistrar, post-transfer authority (new registrar leaves the sender pool, old key reverts), timestamp monotonicity, revoke-preserves-verified-fields. Guard: red-flag output is EXACTLY the first applicable condition against a fresh transcription of the order (verified against journeys.md:32 — they match), degradation monotonicity, extraction never-panics + answers-only-from-real-shapes + constructive-at-any-offset, four zero-storage-delta revert properties (vm.snapshot equality), encoder round-trips. No `prop_assume`/`prop_filter` anywhere — no vacuous skipping.
- **Five GUARD_* strings byte-exact**: constants (guard lib.rs:160), pinned against `packages/shared/abi.ts` GUARD_REVERT_REASONS, asserted by step-3 unit tests (`red_flag_probe_flags_are_byte_exact`, `red_flags_encode_as_error_string` with the literal "GUARD_PAUSED") — unchanged and still passing.
- **Rustdoc**: security-considerations module docs added to both contracts; every public fn on both surfaces carries a doc comment; no TODO/FIXME.
- **Docs judge-grade**: threats.md covers every dispatch ask — beacon-shape extraction assumption + skip-never-guess semantics (§4.2/§4.3), MM-key escrow trust (§4.4: liveness not safety), single-registrar rationale + the honest irreversible-transfer residual risk + transferRegistrar recovery (§3/§3.1), reentrancy walk (§4.1), adversarial-token table, fail-open/fail-closed split, error taxonomy (§4.5), overflow posture (§4.6), 96 KB headroom (§5), chain assumptions (§6). criteria.md sits at exactly the path the registry panel links and states the consumer MUSTs.
- **Gas report**: methodology (hash-join receipts, per-row status+gas gate), PASS-PENDING-FUNDS declared with the verified zero-balance fact, dry-run-measured sizes/fees on both chains, §4 actuals table as pending receipts (the step-3-actuals diff is recorded as exactly that — per plan Revised 2026-09-20 note 1 they never existed), pre-registered drift policy. Spike evidence log exists (`spike/evidence/stylus_check_4663_421614.log`).
- **verify.md's 8 loose ends: all 8 applied.** 1/2/3/5/8 in the findings runbook (registrar funding, transfer-not-deploy path, `--var` no-clobber technique, 421614 writer-of-record, DRIFT_EXTRA_TOKENS → step 9); 4+7 in gas-report.md (PASS-PENDING-FUNDS end-state, docs/ path); 6 by omission — no gas claim in the fuzz suites, assertion kept where measurable (receipts), stated plainly in the fuzz module header.
- **Scope**: no behavior change anywhere (guard's only lib.rs edit is the ponytail comment → prose). No premature `deployments/4663.json`, no `reconciled:` line, findings status honest. Untracked `step-8-e2e-journeys/verify.md` in the tree is another step's file, not on this branch.

## Findings

- [non-blocking] contracts/core/guard/src/lib.rs:655,706,733 — `BEACON`, `mock_record`, `install_beacon_token` are dead in the guard test target (`cargo test -p guard` prints 3 dead-code warnings; present identically on main, so pre-existing, not introduced here). Findings' "warning-free" claim is scoped to the 20 unexpected-cfg warnings, which is accurate, but a judge running `cargo test` sees them. Cheapest cleanup: delete the three unused helpers (or wire them into a test) in a follow-up.
- [non-blocking] contracts/Cargo.lock, contracts/core/hello/Cargo.toml — outside the letter of "contracts/core tests + docs". Both fine: the lockfile is the mechanical shadow of the plan-named proptest dev-dep (wasm32 clean proves it is dev-only), and hello's 3-line feature declaration is consciously disclosed in findings ("Out of scope, left broken"). No action.
- Deferred deploy: NOT a blocker per standing dispatch. The checklist is recorded explicitly as the 7-step funded-run runbook in findings.md (end-to-end deploy.sh per chain → registrar key funded + `transfer_registrar` handoff → append-only 4663.json → `cargo stylus verify` with the Cloudflare fallbacks → worker secrets + `--var` handoff → gas actuals fold-in + PASS-PENDING flip). `deployments/4663.json` addresses marked PENDING for the frontend env handoff.

Verdict basis: plan tasks 1–4 and 6 done and evidenced; task 5 deferred with an executable runbook; tests hold; nothing planned-and-missing besides the funded run itself.
