# Step 9 — Demo harness: the scripted 5-minute arc + submission master assets

## Resources
- spec:    ../product/spec.md   (the arc beat-by-beat with timings spec.md:39; degraded branch spec.md:55; demo network default spec.md:59)
- journeys: ../product/journeys.md  (arc ↔ journey mapping, journeys.md:47)
- knows:   ../knowledge/robinhood-chain.md (RPC/explorer URLs :14–21), ../knowledge/goplus-api.md (copy branch :26–27)
- learned: ../step-3-contracts-core/findings.md, ../step-4-scan-backend/findings.md, ../step-6-replica-assets/findings.md, ../step-8-e2e-journeys/findings.md
- learned: ../step-1-repo-scaffold/findings.md  (scaffold MERGED at 5e2ff35 — live-verified RPC/URL table, production worker+pages already deployed)
- learned: ../../spike/findings.md  (step-2 findings — MERGED at 9d8dc30; file lives in the REPO at `spike/findings.md`: canonical-fetch worker LIVE, calibrated probe targets) + ../step-5-frontend-screens/findings.md  (MERGED at e37bc15 — mock/live env contract for the screens)
- learned: ../step-7-contract-hardening/findings.md  (MERGED at 93201ab — hardening/fuzz/docs/gas done; the ENTIRE on-chain remainder is the 7-step funded-run runbook in that file) + ../step-8-e2e-journeys/findings.md  (MERGED at b17fdd3 — 28 e2e green + 3 activation-gated skips; seed-scratch.sh parse-fixed but never executed)

> **Revised** — step-1 findings reconciliation (2026-09-11): the runbook's endpoints and beat URLs come from the live-verified table in `packages/shared/wire.md` (chains) and `deployments/{worker,pages}.json` — the production worker (`vetted-scan-backend.dujar-coding.workers.dev`) and Pages (`vetted-1un.pages.dev`) have been live since step 1. Caveat carried in findings: per-deployment URL probes intermittently failed TLS from the dev machine only — if a beat URL "fails" during a dry run, re-check from off-machine before rewriting the runbook fallback.

> **Revised** — step-2 + step-5 findings reconciliation (2026-09-19): the power-report beat's blocklist evidence is real only via step 4's server-side BEACON probe (the calibrated blocklist state lives on the beacon; the frontend's live preview row degrades to "probe unavailable" on genuine-pattern tokens — known step-5 defect, see step 8's Revised note 2). Narrate the power report from the `/scan` verdict payload, not the preview rows. The live-scan beat's canonical ground truth is backed by the live canonical-fetch worker (194 assets, re-verified 2026-09-19) absorbed into step 4's worker.

> **Revised** — step-4 + step-6 findings reconciliation (2026-09-20):
> 1. **Scan retries are part of the harness:** the public 4663 RPC rate-limits Cloudflare's shared egress — whole scans intermittently return the DESIGNED `RPC_RETRYABLE` terminal; 10 spaced live attempts on 2026-09-20 produced zero full passes (windows are real but rare). Build retries into `demo/arc.sh` and the runbook fallbacks: on `RPC_RETRYABLE`, wait + re-scan — never narrate a failure; on honest UNVERIFIED, use the zero-fabricated-evidence framing. If throttling persists into the demo window, a paid/private RPC endpoint is the upgrade path — the decision is step 10's (the worker keeps the rpc headroom for it: single-flight elided, 30s in-isolate TTL, `scan-backend/src/rpc.rs`).
> 2. **Capture the still-missing VERIFIED P receipt:** step 4's worker has no VERIFIED-shaped P receipt yet (the honest degrade was verified in its direction instead). Across dry-run windows run `BASE_URL=https://vetted-scan-backend.dujar-coding.workers.dev ./scan-backend/scripts/live-check.sh` with spaced attempts; record the receipt in this step's findings when one lands.
> 3. **Watchdog copy: the spec's 6,092 cumulative / ~150-per-day figures are measured STALE** (live L1 batchCount 258,707; delivered-style events ~2,630/day — step-4 builder measurement, 2026-09-20). Runbook + video-script copy quotes the MEASURED figures WITH provenance (L1 counter read + date). The live `/watchdog` endpoint still serves the degrade sentinel (`runs: 0` + `provenanceUrl` + baseline 150/day — live counter mechanism unpinned, a carried coordinator flag) and the frontend mock numbers (6092/150) stay until a reconciled counter exists: narrate the widget as baseline/illustrative, never as a live measurement.
> 4. **Replica beats reuse step 6's assets, do not re-derive:** `demo/assets.md` §"Beat choreography" has the exact cast commands; the cast deploys CLEAN (unpaused, nobody blocked, 1M AURE to admin) so beats are driven live; `implV2`'s address is the upgrade beat's target (already in the manifest). `scripts/seed-demo/` reuses the step-6 deploy path (`scripts/deploy/replicas/deploy.sh` + the demo/assets.md manifest).

> **Revised** — step-7 + step-8 findings reconciliation (2026-09-21):
> 1. **Prerequisite — the funded-run runbook executes at or before this step's start:** step 7 merged everything except the on-chain run (operator key still unfunded); the entire remainder is the 7-step runbook in `../step-7-contract-hardening/findings.md` — fund the operator key → end-to-end `deploy.sh --chain 421614` (first funded run anywhere = writer of `deployments/421614.json`) → registrar key `cast wallet new` + funded on 4663 → 4663 `deploy.sh` + `transfer_registrar` handoff → `deployments/4663.json` created (its registry/guard fields are the VITE_* address values; stage F shallow-merges append-only over step 6's replicas field) → `cargo stylus verify` (Cloudflare fallbacks: browser-UI Blockscout / sourcify) → worker secrets + vars → gas actuals fold-in. Task 6's dry run REMAINS the first full journey pass on live 4663 — it runs after the runbook, not instead of it; the runbook itself needs no step-9 build work (any builder can execute it; nothing else is missing). If only one chain got funded it was spent on 4663 (runbook rule) — the 4663 beats are unaffected.
> 2. **The demo registrar is the NEW post-handoff key:** `deploy.sh` deploys with registrar = DEPLOYER and hands over via `transfer_registrar`; after the handoff, registrar-only writes revert under the old key. Task 1's seeding and any revocation beat sign with the funded `cast wallet new` key — the same key that went into the worker's `REGISTRAR_KEY` secret. The manual drift-check beat (task 3) needs the runbook's `DRIFT_ADMIN_SECRET` set — until then `/registry/drift-check` degrades to a skipped report.
> 3. **Worker reconfiguration re-passes ALL known non-empty registry vars on the command line:** this step owns `DRIFT_EXTRA_TOKENS` (runbook 6d — when a beat needs the replica tokens inside the drift walk): `wrangler deploy --var DRIFT_EXTRA_TOKENS:… --var REGISTRY_ADDRESS_4663:0x…` (+ the scratch pair if the scratch activation ran). Never edit `wrangler.toml` + redeploy from a stale checkout — a redeploy without the `--var` flags silently blanks the live `[vars]` (verified 2026-09-21: all four are `[vars]` entries at `scan-backend/wrangler.toml:13-21`; worker live and shared). Note: the runbook text "step 8 re-passes" is stale — step 8 merged before the runbook ran and left the vars empty; the first real pass is the runbook's, then this step's.
> 4. **seed-demo inherits e2e's settle plumbing; the scratch activation path:** `e2e/scripts/seed-scratch.sh` wires the J2 swap plumbing (buyer tokenIn balance+allowance, MM tokenOut balance+allowance) — it had a bash parse error (funding-gated, never executed), now `bash -n` clean but STILL never executed; task 1's swap beat (genuine settles on 4663) needs the same two-sided wiring — port the pattern, first real execution happens in this step. Step 8's 3 activation-gated live-scratch skips activate per the `e2e/README.md` runbook (:41: funded deploys → worker vars → seed → `--project=live`) from funded `deployments/*.json`; after the runbook, run the live e2e project once — the honest-skip→green flip is the cheapest journey-wide regression check before recording.
> 5. **Narration honesty (step-8 residue):** the registry UI's live drill-in keeps `revocationTx: null` ("not indexed yet" — only `/scan` attaches the revocation tx), so revoke evidence is narrated from the `/scan` payload, never the registry table; the J3 empty-registry state is unit-pinned only (`frontend/tests/registry.test.tsx:76` — coordinator-carried residue, forcing it e2e needs an out-of-scope frontend hook) and the demo always shows a seeded registry, so narration must not claim the empty state was journey-verified. The live Pages bundle (redeployed 2026-09-21) already carries `VETTED_CHAIN`/`VITE_CHAIN_ID` and the render-loop lazy-singleton fix.

## Stack
Bash + cast + OBS recording; no new runtime dependencies.

## Goal
After this step the 5-minute arc runs start-to-finish on 4663 from a fresh browser with a runbook in hand: every URL/address/keystroke pre-staged, the upgrade→auto-revoke→guard-refusal beat reliable via the manual drift-check endpoint, and the video script + repo README ready for submission.

## Scope
- Creates: `demo/**` (runbook, beat scripts, video script, recording), `scripts/seed-demo/` (idempotent registrar seeding).
- Out of scope: product fixes (go to owning steps); submission-form mechanics (step 10).

## User journey
The demo arc IS the scripted journey (spec.md:39, mapping journeys.md:47): docs-page problem (30s) → live read-only scan of a real 4663 address we did NOT deploy (30s) → impostor twin vs canonical compare deep link (30s) → power report on the genuine replica: hidden blocklist, global pause, beacon-upgradeable (45s) → guarded swap: impostor reverts with the reason surfaced, genuine settles (45s) → **the beat no incumbent has:** beacon-upgrade the canonical replica → registry auto-revokes → the guard refuses the formerly verified token (60s) → watchdog widget + registry-as-primitive + roadmap close (30s).

## Screens
N/A — orchestration over existing screens.

## Tasks
1. `scripts/seed-demo/`: registrar-signed, idempotent seeding of 4663 demo state — verify(canonical replica); revoke(one record, for the REVOKED states); twins left unregistered; uses the same registrar code path as step 4 — signed with the NEW post-handoff registrar key, and including the swap-settle plumbing (buyer tokenIn, MM tokenOut — balance+allowance both sides) ported from `e2e/scripts/seed-scratch.sh` (Revised 2026-09-21 notes 2 and 4).
2. Beat runbook (`demo/runbook.md` + `demo/arc.sh`): per beat — URL, wallet/account, expected screen state, timing budget, and the fallback (what to say and click if fetch/RPC/wallet misbehaves: the degraded branch is pre-written copy, spec.md:55; the live-scan beat carries a backup address list; scan retry on the designed `RPC_RETRYABLE` terminal — Revised 2026-09-20 note 1).
3. Make the upgrade beat reliable: script the beacon-upgrade tx + the manual `POST /registry/drift-check` call + the guard refusal; measure wall-clock. If cron latency would eat the 60s beat, the runbook uses the manual endpoint and the narration says the cron runs the same code on schedule — honest, not staged. (The manual endpoint needs the runbook-set `DRIFT_ADMIN_SECRET`, and this step owns passing `DRIFT_EXTRA_TOKENS` via `--var` when a beat needs the replicas in the drift walk — Revised 2026-09-21 notes 2–3.)
4. Video script (`demo/video-script.md`) mapped 1:1 to beats, narration hitting the judged criteria (enforcement, revocation, power disclosure — never "trust layer", never "first coverage": spec.md:4, knowledge goplus-api.md:26–27); watchdog figures quoted from the MEASURED L1 numbers with provenance, not the stale 6,092/150 spec copy (Revised 2026-09-20 note 3); record the full take + per-beat clips.
5. README final draft: architecture diagram, quickstart, `docs/criteria.md` link, demo gif, demo-cast labeling (spec.md:80) — repo hygiene feeding the contract-quality and real-problem-solving criteria.
6. Full dry run on a fresh browser profile, timed; actuals vs budget logged in the runbook. **Prerequisite: the funded-run runbook (`../step-7-contract-hardening/findings.md`) has executed** — `deployments/4663.json` exists, the worker vars/secrets are set (Revised 2026-09-21 note 1). Between timed takes, run `scan-backend/scripts/live-check.sh` with spaced attempts to capture the still-missing VERIFIED-shaped P receipt on a calm window (Revised 2026-09-20 note 2). This dry run is the **first full journey pass on live 4663** — step 8's e2e suite greened against the step-3/6 scratch deployments (46630/421614); treat any 4663-only divergence (RPC behavior, explorer links, verdict on the real cast) as a blocking fix before recording.

Check: the arc completes ≤5:00 twice in a row from the runbook; video recorded; README renders with resolving links.

## Open questions
- The final product name must be locked BEFORE recording (state.md:40 defers it to the coordinator at screens phase) — flag at step start; recording waits on it.
