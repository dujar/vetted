# Review — step-1-repo-scaffold (round 1, 2026-09-11)

Diff reviewed: `main...step-1-repo-scaffold` (64 files, +12,080). PR #1 on dujar/vetted, base main, all four CI jobs SUCCESS on GitHub. All four test suites re-run locally and green: contracts 2, scan-backend 3, shared 15 TS + 3 Rust, frontend 3; `tsc && vite build` and the wasm32 workspace build pass.

## Verdict

APPROVED — no blocking findings, 4 non-blocking notes.

## What was checked and holds

- All 9 tasks built and verified: repo+.gitignore; toolchain pins (`rust-toolchain.toml` `channel = "stable"` + wasm32, `scripts/toolchain.sh` pins cargo-stylus exactly 0.10.9, no protoc step); contracts workspace + `core/hello` with `#[entrypoint]` and native `stylus_sdk::testing` tests; scan-backend workers-rs hello (`GET /health` → `{"ok":true}`) deployed live with URL in `deployments/worker.json`; frontend (vite 8.3/react 19.3, `@tailwindcss/vite` 4.3.3 CSS-first with no postcss config, wagmi 3.7.7 + viem 2.56.3 + react-query, AppKit excluded, three-chain `chains.ts`, byte-exact token transcription of theme.css `:root`, primitives, VerdictBanner vitest covering all four verdicts + variant mapping); `packages/shared` wire contract; four-job CI; Pages deployed and recorded; README stub with layout map + build order.
- Wire pins are byte-consistent across plan / wire.md / types.ts / types.rs / abi.ts: verdict enum; the five `GUARD_*` reasons verbatim (abi.ts:40–46 = journeys.md:32); seven-field record {status, riskFlags, verifiedAt, impl, registrar, revokedAt, reason}; scan request as query params `{chainId, addr}`; `verify(address token, uint256 riskFlags, address impl)` carried identically into step-3 plan.md (the one cross-step edit, ordered by verify.md loose end 1). `SELECTORS` literals are not trusted on faith — `roundtrip.test.ts:87–98` recomputes them with viem `toFunctionSelector`.
- Tests are not vacuous: TS round-trip asserts byte-identical re-serialization against canonical fixtures plus per-verdict invariants (REVOKED carries record + 66-char revocationTx, VERIFIED has `revokedAt: 0`, every power row has an http evidence URL); Rust side asserts structural round-trip + full five-reason coverage; backend tests hit 200/405/404 through the pure `route` fn (loose end 5 applied exactly — no wasm-bindgen types under `cargo test`); frontend test asserts the class/variant mapping, not just that render doesn't throw.
- All 10 verify.md loose ends applied: 1 (signature fixed in both plans), 2 (seven fields), 3 (46630 RPC recovered from docs asset bundle + live-probed 0xb626, evidenced in wire.md chains table and findings.md:15), 4 (Panel = `.panel` card mapping and advisory Skeleton, both documented in-file), 5 (pure-fn route tests), 6 (premise stale — token had scopes; both deploys LIVE and recorded, stronger than the blocked-auth fallback), 7 (PUBLIC decided in plan.md:46), 8 (`channel = "stable"`), 9 (lazy env-gated `getWagmiConfig()`, no-projectId boots read-only; the vitest file imports only the component), 10 (query-param request pinned).
- Dispatch's three checks: `findings.md` present with a real build record (status ready-to-merge, five plan-correction notes incl. the serde `GUARD_RECORD_REVOKED` rename and the worker-build correction); WalletConnect projectId is the only remaining operator action (findings.md:24); no premature `reconciled:` line anywhere (grep: only the standing rule in step-feature-state.md:27).
- Scope held: no screens (App.tsx is a marked scaffold shell), no contract logic beyond the hello placeholder, no backend rules beyond `/health`. No other steps' reserved files touched.

## Non-blocking notes

- [note] branch tree — `e2e/` and `demo/` exist only in the local worktree, not in the pushed branch (git does not track empty dirs; `contracts/replicas/` got a `.gitkeep`, these two did not) — a clean checkout lacks the reserved layout the plan's Goal promises — smallest fix: two `.gitkeep` files, or bless the omission in findings.md since the first writer of each creates the dir.
- [note] plan.md:45 — open-questions bullet still says deploys fall back to `blocked-auth` per loose end 6 — the premise proved stale (deploys live) and findings.md records the truth, but the plan text now contradicts the build record — smallest fix: none required (plan is historical); optionally strike the parenthetical.
- [note] frontend/src/components/verdicts.ts:7 — `VERDICTS` union duplicated from packages/shared instead of imported — deliberate and marked with the step-5 switch point, but it can drift until then — smallest fix: step 5 swaps to `vetted-shared` imports as the file comment already states.
- [note] contracts/core/hello — 4 rustc warnings (`unexpected cfg condition value: contract-client-gen`) from stylus-sdk macro expansion, not scaffold code — cosmetic; no action.
