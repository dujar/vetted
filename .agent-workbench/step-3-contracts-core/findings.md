# Step 3 — contracts-core (Canonical Registry + Guarded Swap, Rust/Stylus)

status:     ready-to-merge
branch:     step-3-contracts-core
deployed:   not deployed — operator key 0x151e9f57F31310aFeBBB60c222c14badCf938E4C has balance 0 on 421614, 46630 and 4663 (cast balance, 2026-09-20); the task 5/6 on-chain RUN and its receipts stay deferred per the standing dispatch (code + script are landed and validated as far as funds allow)

## What was built
Review round 1 ruled tasks 1–3 + 7 merge-quality (registry 12 native tests, guard 21, events tripwire 3 vitest) but blocked on the missing task 4/5 code (finding 1). This session landed it:

- `contracts/core/mock-token/` (foundry project, excluded from the cargo workspace per plan Revised note 4) — the mock pattern tokens: forwarders whose runtime IS the genuine 283-byte `spike/evidence/p_proxy.hex` bytecode with only the embedded beacon patched (`ForwarderPatch.sol` mirrors the guard's `extract_beacon_from_code` algorithm opcode-for-opcode; `RawHull.sol` = EIP-5202 hull deploy; test tripwires the constant against the evidence file byte-for-byte); ONE shared beacon + ONE shared impl (genuine layout, calibration_4663.json) with the blocklist ON the beacon (`isBlocked` 0xfbac3951) and pause on the TOKEN (per-proxy via delegatecall storage); the impl deliberately has NO `isBlocked` (reverts via proxy/impl, as calibrated); an unwired v2 upgrade target; a plain no-pattern token for the GUARD_NO_RECORD receipt and the degraded (extraction-fails → skip) settle. `script/RunReceipts.s.sol` runs all five red flags ON-CHAIN with byte-exact `Error(string)` expectReverts + degraded + clean settle with balance assertions (11 guard executes: 5 revert at indexes 1/3/5/7/9). 13 forge tests.
- `scripts/deploy/core/deploy.sh` — `--chain 4663|46630|421614` (RPCs from wire.md, not re-probed), deploys registry (registrar=deployer) + guard (registry, MM_ADDR) via `cargo stylus deploy --constructor-args` with post-deploy `cast call` validation, runs the forge stages, asserts the ≤200k gas gate from the broadcast `receipts` array (joined to transactions by hash, hex→dec in bash), writes `deployments/<chain>.json` append-only (shallow merge, never clobbers step-6 fields), `--verify` per plan task 6 (Etherscan v2 chainid=421614 / Blockscout for 46630, BLOCKSCOUT_URL env for 4663), `--skip-receipts` / `--receipts-only`.
- CI: `mock-token` job (forge test with pinned forge-std v1.16.2 clone, same pattern as the replicas job).

Validated this session, no funds needed: forge 13/13; cargo 35 + vitest 21 + scan-backend 45 (all suites green on the rebased branch); `cargo build --release --target wasm32-unknown-unknown` clean; mock-only anvil rehearsal of DeployMockTokens (console parse format + `receipts` schema + gas-gate jq proven against a real broadcast artifact + synthetic join test); `cargo stylus deploy --estimate-gas` for BOTH crates against live 421614 — docker reproducible build, size 11.1/12.0 KB < 96 KB cap, live ArbWasm fee quote, `--constructor-args` encoding accepted.

**Rebased onto main 559344c** (step-4 merged mid-session) with two small reconciliations at the shared append points (3d5a261): the mock-token CI job now uses `foundry-toolchain` (main's 786c762 fixed the nonexistent `foundry-action` this job had copied from the pre-fix replicas job), and step-4's stale "events.ts ships with step-3" comment in `packages/shared/index.ts` is dropped — the real module + export ship with this merge, closing step-4's dangling-reference fix properly.

## Where the plan was wrong
Nothing new this session; round 1's dispositions stand. Two scope/reality notes for the record: (1) the CI edit (`.github/workflows/ci.yml`) sits outside the tracker's step-3 file scope but the tests cannot run in CI without it — precedent: step 6's b0eab8b did exactly this for its foundry project; (2) mock-token identity is per-proxy storage (`init` through each forwarder) with the impl's `name`/`symbol` storage-backed — Solidity immutables can't be strings and a shared impl means per-token state MUST live proxy-side, which is the genuine semantics anyway.

## What the next step needs to know
- **Funding is the only remaining manual step** (`spike/DEPLOY.md` — faucet links; headless 46630 funding documented impossible, `spike/evidence/funding_blockers_20260919.txt`). When funded: `DEPLOY_KEY=0x… ./scripts/deploy/core/deploy.sh --chain 421614` produces the five revert receipts + degraded + settle, the ≤200k assertions, `deployments/421614.json`, and (with `--verify`) the verified pages. `--receipts-only` re-runs receipts against a prior deploy. `MM_KEY` distinct from `DEPLOY_KEY` gives the two-party settle accounting (one-key fallback = self-fill, logged).
- **Residual risk, honestly:** RunReceipts has NOT run end-to-end (no usable stylus devnode image locally — `offchainlabs/stylus-devnode` not pullable, local nitro-node-dev build rejects `--dev`/exits). Deploy-side is validated read-only (see above); the forge expectRevert-under-broadcast pattern is standard foundry. If the funded run hits script friction, it is re-runnable without redeploying.
- **Review round 2 (APPROVED) routing requirement + dispositions:** the FIRST funded run must go through `deploy.sh` end-to-end so stage D/E executes and any broadcast-JSON shape drift surfaces BEFORE steps 7/8 consume `deployments/` — record the receipts there as step 3's committed gas actuals (step-7 task 2 diffs against them) [round-2 note 1, routed]. Stage G source-verifies only MockBeacon + MockTokenImpl; the hull forwarders are unverifiable by construction (raw-runtime deploys) — stated in mock-token/README.md now [round-2 note 2, taken]. `deployments/README.md` pins 4663.json writers as 6/7 while deploy.sh accepts `--chain 4663`: per plan step 3 never runs there (task 5 targets 421614+46630; 4663 is step 7's, via the same script) and the merge writer cannot clobber — no action [round-2 note 3, no action].
- Step 7 consumes this step: same deploy script for the 4663 core deploy, gas diffs vs these receipts, and the receipts eventually assert what the native tests cannot (probe→target wiring, zero-movement on all five revert paths — round 1 findings 2+3 deferrals).
- Events/beacon/consumers: unchanged from round 1 — `packages/shared/events.ts` topics; record.reason = keccak256(utf8(reason)), full text rides Revoke; guard skips (never reverts) on extraction failure, GUARD_IMPL_MISMATCH only on successful mismatching resolution.

## Out of scope, left broken
- `cargo build` warnings (8 in registry/guard lib targets) — step 7's quality pass.
- guard/src/lib.rs:588 leftover `ponytail:` marker — step 7 (round 1 finding 4).
- The two untracked verify.md files in the root worktree belong to steps 4/6 — untouched.
