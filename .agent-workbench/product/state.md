# State

target: spec.md
phase: 6 — audit — CLOSED (round 2 landed 2026-09-11; all findings fixed round 4)
round: 4
market-agent: 1 run (2026-09-11) — DONE, loop closed (all sharpening questions answered; no new competitive surface opened)
judge-agent: 1 run (2026-09-11) — DONE, loop closed (holes 1–6 all adopted into spec round 2)
audit: round 1 (2026-09-11) 19 gaps → all fixed round 3. Round 2 (2026-09-11): journeys GREEN; spec 3 gaps, screens 3 gaps → all six fixed round 4 using the audits' own prescribed smallest fixes. Two-round ceiling reached — no third pass; fixes were applied verbatim to prescription and self-verified by inspection (residue grep clean), not re-audited. All three scopes resolved. DONE.
waiting on: nothing — approved, proceed to knowledge
approved: 2026-09-12 — granted by the coordinator on the user's standing delegation ("I will let you make the decisions from now onwards while the product is being crafted until full implementation", 2026-09-12); includes the Q7 AMENDED deploy target (Robinhood Chain 4663 mainnet + Arbitrum Sepolia mirror), with 4663 gas acquisition added to the week-1 spike as the verification gate

## Open
- (none — every decision made under the user's delegated authority; do not re-queue Q1–Q8 or judge/market questions)

## Answered
- [x] Direction · Build the trust layer for Robinhood Chain tokenized stocks (scout candidate 1) — chosen by user · round 0
- [x] Target · Win First Place Overall ($40,000 USDC); innovative, production-grade stack — user · round 0
- [x] Delegation · ALL product decisions delegated to coordinator; decide from evidence, never re-queue — user · round 0
- [x] Hackathon constraints · Arbitrum Open House Singapore; deploy on an Arbitrum chain (Robinhood Chain 4663 qualifies); Solidity or Rust/Stylus; close 2026-10-04 23:59 SGT; judged on contract quality, PMF, innovation, real problem solving — verified · round 0
- [x] Q1 · On-chain enforcement → Registry + guarded swap with execution-time probes; deterministic red flags revert, heuristics warn in report only · round 2 (coordinator)
- [x] Q2 · Demo assets → Self-deployed faithful replica set (beacon proxy + hidden modifier blocklist + global pause, verified canonical; impostor twins); scanner still accepts any live address · round 2 (coordinator)
- [x] Q3 · Contracts → Rust/Stylus everything on-chain; Solidity fallback only if Robinhood Chain lacks Stylus; exact day-7 pass/fail gate committed in spec §Tech (probe staticcall + code read + EIP-1967 slot, ≤200k gas) · round 2 (coordinator)
- [x] Q4 · Registry writer → single registrar key held by project backend, records revocable, criteria published in repo · round 2 (coordinator)
- [x] Q5 · Scan engine → thin stateless Rust backend on Cloudflare Workers (bytecode, EIP-1967, probes, rule engine); React frontend · round 2 (coordinator)
- [x] Q6 · v1 cut → core three PLUS censorship-watchdog stats widget (one RPC read) on scan page, promoted from roadmap per judgment H4 / market Q14; watchlists + alerts stay roadmap · round 2 (coordinator)
- [x] Q7 · Deploy → Robinhood Chain testnet + Arbitrum Sepolia mirror, same deploy scripts · round 2 (coordinator) — **AMENDED round 4, see below**
- [x] Q8 · Theme → go deeper: dark "security terminal", verified-green vs risk-red duality; written as tokens in theme.css · round 2 (coordinator)
- [x] Judgment H1–H6 → all adopted: §Problem rewritten around coverage ≠ correctness ≠ enforcement + live GoPlus re-test in week-1 spike with pre-written copy branch (H1); demo includes read-only scan of a live non-self-deployed address + IMPOSTOR ground truth wired to docs.robinhood.com/chain/contracts fetched live (H2 / market Q10); beacon-upgrade→revocation→guard-refusal beat added to demo arc (H3); PMF = registry-as-primitive + watchdog widget (H4); depth boundary stated in UI and spec §Verdict-discipline 3 (H5); "trust layer" dropped from one-liner, "why not just EAS?" paragraph written + EAS check in spike, "attestation" renamed → Canonical Registry / verification record / registrar (H6 / market Q11–Q13) · round 2 (coordinator)
- [x] Market Q15 · Token Sniffer / De.Fi unverified capabilities → recorded as pre-submission browser spot-check task in spec §Constraints checklist (operational task, not a design decision) · round 2 (coordinator)
- [x] Audit fix-ups (all 19 round-1 gaps, coordinator) — canonical-list fetch added as fifth spike bullet with pre-committed degraded branch + Risk-4 tie-in; revocation trigger = backend Workers cron polls each record's impl pointer vs the live beacon slot, revokes on drift, registry-as-state (no DB), guard probe covers poll-to-revoke window; "tooling vacuum" → "verification-and-enforcement vacuum (analytics exist — Bubblemaps)"; out-of-v1 integrations qualified to keep the registry's public read interface in v1; watchdog trend = published 6-week baseline constant, Workers KV option declined; scan network chip = UI selector defaulting to 4663, J1 wallet-free, wrong-chain handling moved to J2; J1 gains REVOKED verdict + compare deep link (?addr=0xA…&addr=0xB…) + switch-to-4663 unhappy path in J2; scan.html gains REVOKED state + wrong-network error card, .eviline folded into .report-row, chips mono; swap.html gains confirming + rejected/gas states; registry.html gains registrar column + registrar address + loading skeleton + roadmap strip; components input 280px → 32ch · round 3 (coordinator)
- [x] Q7 AMENDED · Deploy → **Robinhood Chain 4663 itself** (public chain, permissionless deploy — scout.md:14 confirms permissionless deploy on the public chain) **+ Arbitrum Sepolia mirror unchanged**. Evidence: the demo's replica-scan and guarded-swap beats already route to 4663 (Q2 replicas verified canonical; journeys network selector default 4663; journeys.md:19 stock-token verdicts only on 4663) — "testnet" was the one unverified residue. New spec Risk 6 records the impostor-twins-on-production hazard with mitigation. Made under the round-0 delegation ("decide from evidence, never re-queue"); flagged in the round-4 receipt for coordinator veto before planning. · round 4 (coordinator-delegated)
- [x] Q7 rationale corrected (knowledge round) · knowledge/robinhood-chain.md verified a Robinhood testnet DOES exist (chain **46630**, rpc.testnet.chain.robinhood.com, Blockscout explorer, bridge contracts — docs.robinhood.com/chain/connecting + /chain/protocol-contracts); the "no testnet exists" premise above was false. **Decision stands, rationale corrected:** primary deploy = mainnet 4663 because the genuine stock tokens the scanner must attest and scan live live there (testnet 46630 does not carry them); testnet 46630 = scratch environment for replica rehearsal before the mainnet deploy; gas on 4663 via the documented Arbitrum portal bridge, acquisition verified in the week-1 spike. · round 5 (coordinator)
- [x] GoPlus spike pre-answered (knowledge round) · knowledge/goplus-api.md: the live endpoint is `/api/v1/token_security/{chain_id}` (underscores — the hyphenated path in earlier memory 404s for every chain); on 4663 it returns OK with only ~16 metadata fields for a genuine stock token (`is_proxy=1`) and **omits every risk field** per their documented proxy caveat. Coverage yes, correctness on this pattern no — the H1 copy branch "enforcement + revocation + power disclosure (never 'first coverage')" is the live-verified branch; the week-1 spike item is downgraded to a screenshot-for-slides re-run, not an open risk. · round 5 (coordinator)
- [x] Round-2 audit fixes (remaining 5, all mechanical, per audit-spec.md / audit-screens.md prescriptions) — spec: one-liner gains REVOKED as the fourth verdict; watchdog reworded to "cumulative filterer runs in one RPC read vs the baseline expressed as a daily rate" in spec §Scope 4 and journeys J1 step 4, with the "173 / last 24h" cell replaced by "6,092 runs since launch — one RPC read" in scan.html and components.html (two numbers only, no stored history); screens: swap.html gains wrong-network state (switch-to-4663 before quoting, J2 unhappy path); scan.html REVOKED revocation tx wrapped in evidence link (standing rule: every verdict line links its evidence); registry.html .skel moved to tokens (height var(--fs-0), ch widths) · round 4 (coordinator-delegated)

## Retired
- (none)

## Deferred
- Product name — coordinator picks at screens phase; "Vetted" placeholder kept everywhere including mockups.
- ZeroDev (AA) gasless UX as demo polish — stretch only after core demo is safe.
- Multi-venue expansion (xStocks etc.) — post-hackathon.
